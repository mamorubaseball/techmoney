# TechMoney ディレクトリ構造の整理ルール

このドキュメントは、`/Users/mamoru/techmoney` 配下のファイルやディレクトリを綺麗に保つための「あるべき姿（理想の構造）」を定義したものです。
直下にファイルが散乱するのを防ぎ、プロジェクトの保守性を高めます。

## 理想のディレクトリ構造

```text
/Users/mamoru/techmoney/
├── .agent/                 # Agent関連（スキル、ワークフロー）
├── assets/                 # 動画素材や参考資料
│   ├── images/             # サムネイル画像など (.jpeg, .jpg, .webp)
│   └── docs/               # レポートや参考資料 (.pdf, .docx)
├── templates/              # テンプレート類
│   ├── prompts/            # LLM用プロンプト (prompt_v2.md, prompt_chapt_read.md など)
│   └── formats/            # 出力フォーマット (gaiyou.md, design.md, sound_gaiyou.md など)
├── scripts_draft/          # まだ月別・銘柄別ディレクトリに格納されていない単体の台本(.md)
├── youtube-to-blog/        # ブログ自動生成システムの開発・管理ディレクトリ
├── 4月/                    # 月別アーカイブ（完了した動画プロジェクト）
├── 5月/                    # 月別アーカイブ（完了した動画プロジェクト）
├── rules.md                # 運用ルール（直下配置）
└── ディレクトリ構造.md       # このドキュメント（直下配置）
```

## 整理の方針（自動化スクリプトの動作定義）

1. **アセットの移動**: 
   - ルートに存在する画像ファイル（`.jpeg`, `.jpg`, `.webp`, `.png`）は `assets/images/` へ移動。
   - ルートに存在するドキュメント（`.pdf`, `.docx`）は `assets/docs/` へ移動。
2. **テンプレートの移動**:
   - `prompt` という名前が含まれるファイルは `templates/prompts/` へ移動。
   - `gaiyou` が含まれるファイル、および `design.md` は `templates/formats/` へ移動。
3. **台本草稿の移動**:
   - 上記に該当せず、直下に置かれている `.md` ファイル（例: `宇宙.md`, `robo_tax.md` など）は、一時的に `scripts_draft/` へ移動。
   - ※ `rules.md` と `ディレクトリ構造.md` はシステムの根幹となるため移動させず直下に残す。
4. **ディレクトリの統合**:
   - 個別の銘柄名ディレクトリ（`セレブラス+20260516`など）は、本来は `5月/` のような月別ディレクトリに入れるのが理想ですが、手作業での確認が必要なためスクリプトでは一時的にそのままにするか、必要に応じて手動で移動します。
